package com.THHSEI.{packageName}.dao;

import java.util.List;
import java.util.Map;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

/**
 * {classComment}
 * @author {classAuthor}
 * @date {classDate}
 */
@Mapper
public interface {daoPrefix}_{className} {

	
	//此注释不要删除，用于自动生成代码
    
    
    
    
}
